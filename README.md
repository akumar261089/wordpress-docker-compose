# Setup wordpress with mysql by dockercompose.

## Start Cluster
 ```
 docker-compose up -d
 ```
Workpress URL `localhost:8000`

phpmyadmin URl `localhost:8080`

## To Tear Down
``` 
docker-compose down --volumes
```
